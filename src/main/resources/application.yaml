server:
  port: 8000


spring:
  #  数据库连接配置
  datasource:
    url: jdbc:mysql://192.168.3.149:3306/test?useUnicode=true&characterEncoding=UTF-8&useSSL=false&autoReconnect=true&failOverReadOnly=false&serverTimezone=GMT%2B8
    username: root
    password: asdASD123.
  servlet:
    multipart:
      max-file-size: 50MB

#minio配置
#配置访问的地址
#用户名，密码
minio:
  endpoint: http://192.168.3.127:9000
  accessKey: admin
  secretKey: admin123

#mybatis 配置
mybatis:
  type-aliases-package: com.example.mysql1.entity
  mapper-locations: classpath:mappers/*Mapper.xml
  #  驼峰命名转换
  configuration:
    map-underscore-to-camel-case: true
debug: true

